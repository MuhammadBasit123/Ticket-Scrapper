# Ticket-Scrapper
Most "extract structured data from text" demos stop at the happy path. This one is built around what happens when the input isn't clean — ambiguous fields, missing data, and text that isn't a support ticket at all — and proves it with a labeled accuracy benchmark instead of a cherry-picked example.
